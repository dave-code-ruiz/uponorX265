# Pinned rather than floating: pytest-homeassistant-custom-component tracks
# Home Assistant betas, and 0.13.358+ pulls HA 2026.9.0bN, which promotes
# `device_registry.async_get_device` from a deprecation warning to a hard
# RuntimeError. The integration and 11 of these tests still call it, so the
# suite goes red on the beta. 0.13.357 is the newest release pinning a stable
# HA (2026.8.3). Unpin once the async_get_device calls are migrated to
# async_get_device_by_identifier.
pytest-homeassistant-custom-component==0.13.357
pytest-asyncio
tzdata; sys_platform == "win32"

# The integration's own manifest requirement - imported at module scope by
# custom_components/uponorx265/__init__.py, so collection fails without it.
getmac
